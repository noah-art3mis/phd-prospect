.env
.env.*
!.env.example
node_modules/
.DS_Store
notion-data-sources.json

# Personal seed data extracted from private notes
seeds/*.json

# Import-ready workflows with personal IDs substituted

# Local Notion data snapshot (real personal data, never commit)
notion-snapshot/

# Python bytecode left by the retired package
__pycache__/
.pytest_cache/

# Local database and its backups – runtime state, not source
data/
