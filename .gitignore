.env
.env.*
!.env.example
node_modules/
.DS_Store

# Local database and its backups – runtime state, not source
data/

# Identifiers for the retired Notion workspace – kept locally, never committed
notion-data-sources.json
