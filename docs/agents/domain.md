# Domain documentation

Prospect uses a single domain context.

Before changing behavior:

- Read `CONTEXT.md` for the shared language and invariants.
- Read relevant architecture decisions in `docs/adr/`.
- Use the glossary's terms in code, tests, issues, and documentation.
- Surface conflicts with an ADR rather than silently overriding it.
